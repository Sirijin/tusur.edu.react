import React from 'react';
import NavBar from './NavBar';
// Импортировать стили для TheoryPage, если необходимо

const TheoryPage = () => {
    return (
        <div>
            <NavBar />
            <h1>Теоретические материалы</h1>
            {/* Здесь будет контент страницы с теоретическими материалами */}
        </div>
    );
};

export default TheoryPage;